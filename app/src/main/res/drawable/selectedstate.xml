<?xml version="1.0" encoding="utf-8"?>
<selector
    xmlns:android="http://schemas.android.com/apk/res/android">


    <item android:state_checked="true">

        <layer-list>
            <!-- Bottom blue color layer. -->
            <item android:start="15dp"
                android:end="15dp">
                <color android:color="@color/colorWhite" />
                <shape>
                    <corners android:radius="35dp" />
                </shape>
              </item>
            <!-- Top orange color layer. -->
            <item android:bottom="3dp" android:drawable="@color/newColorPrimary" />
        </layer-list>

    </item>

    <!--If radio button is unchecked. -->
    <item android:state_checked="false">
        <layer-list>
            <!-- Bottom blue color layer. -->
            <item android:start="15dp"
                android:end="15dp">
                <color android:color="@color/colorWhite" />
                <shape>
                    <corners android:radius="35dp" />
                </shape>
            </item>
            <!-- Top orange color layer. -->
            <item android:bottom="0dp" android:drawable="@color/newColorPrimary"/>
        </layer-list>
    </item>
</selector>